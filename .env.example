# Server port (default 5000). Set on AWS to match your platform (e.g. 8080).
PORT=5000

# Database URL for migrations and db:push. Configure when using Supabase/Postgres.
# Leave unset to run app with in-memory storage only.
# DATABASE_URL=postgresql://user:password@host:5432/database
